# Build output. Everything here is generated.
/dist/
build/
/target/
out.ll
*.o
*.exe
*.pdb
*.dmp
# The committed bootstrap seed lives at tools/seed/orion.ll; stray IR does not.
/*.ll
*.host.ll

# Scratch the test runner writes beside itself.
*.tmp
# written by test_42_proc_parallel, into whatever directory it runs from
.proc_*
/.dbg_code.txt
tests/suite/.*.txt
tests/suite/.*.bat

tests/suite/dist/

__pycache__/
.claude/
