data Item: name: Text, value: int
data Bag: items: [Item]

fn main() -> int:
    a = Item{name: "alpha", value: 21}
    b = Item{name: "beta", value: 21}
    bag = Bag{items: [a, b]}
    first = at(bag.items, 0)
    second = at(bag.items, 1)
    first.value + second.value
