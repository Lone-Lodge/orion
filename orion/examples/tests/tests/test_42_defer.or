fn step(label: Text):
    print_line(label)

# Defer runs registered exprs in LIFO order at block end.
fn main() -> int:
    defer step("[3] cleanup C")
    defer step("[2] cleanup B")
    defer step("[1] cleanup A")
    print_line("doing work")
    42
