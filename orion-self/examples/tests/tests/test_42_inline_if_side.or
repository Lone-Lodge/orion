fn track_42() -> int:
    print_line("then")
    42

fn track_0() -> int:
    print_line("else — should NOT print")
    0

fn main() -> int:
    # Inline if-then-else with side effects — only one branch should run.
    cond = 1
    if cond == 1 then track_42() else track_0()
