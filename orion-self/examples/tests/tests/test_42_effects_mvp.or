effect Echo:
    n: fn(input: int) -> int

# Handler — perform Echo.n(input) dispatches via setjmp/longjmp.
# For now MVP: handler returns directly (no resume_int).
fn __handler_Echo_n(input: int) -> int:
    input

fn main() -> int:
    perform Echo.n(42)
