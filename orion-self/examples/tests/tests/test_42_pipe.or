fn double(x: int) -> int:
    x * 2

fn inc(x: int) -> int:
    x + 1

fn main() -> int:
    # 10 |> inc → 11
    # 11 |> double → 22
    # 22 + 20 → 42
    result = 10 |> inc |> double
    result + 20
