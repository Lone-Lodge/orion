use orion_lex
use orion_parse

fn main():
    src = "data Point: x: int, y: int\n"
    tokens = self_lex(src)
    print_line("tokens: {len(tokens)}")
    ast = self_parse(tokens)
    print_line("done")
