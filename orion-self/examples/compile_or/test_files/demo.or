fn id<T>(x: T) -> T:
    x

fn main() -> int:
    a = id(20)
    b = id("hello")
    # a should be Int. If HM works, a + 22 typechecks; if a is Unknown,
    # also passes. Real proof: try Text into Int-expecting position.
    a + 22
