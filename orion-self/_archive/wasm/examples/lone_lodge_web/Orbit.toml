[package]
name = "lone-lodge-web"
version = "0.0.1"
description = "Full lone-lodge stack running in the browser — Veil + Atlas + strings, pure-Orion wasm"

[orbs]
orion_emit_wasm = "path:../../orbs/orion_emit_wasm"
orion_veil_to_wasm = "path:../../orbs/orion_veil_to_wasm"
bytes = "*"
io = "*"
fs = "*"
