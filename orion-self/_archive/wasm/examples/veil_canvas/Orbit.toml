[package]
name = "veil-canvas-demo"
version = "0.0.1"
description = "Veil — pure-Orion display list rendered to a browser canvas"

[orbs]
orion_emit_wasm = "path:../../orbs/orion_emit_wasm"
bytes = "*"
io = "*"
fs = "*"
