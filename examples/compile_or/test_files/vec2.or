type Vec2: x: number, y: number

define make_vec(x: number, y: number) -> Vec2:
    Vec2{x: x, y: y}

define dot(ax: number, ay: number, bx: number, by: number) -> number:
    ax * bx + ay * by

define main() -> number:
    v = make_vec(3, 4)
    d = dot(v.x, v.y, v.x, v.y)
    d
