type Vec2: x: int, y: int

define make_vec(x: int, y: int) -> Vec2:
    Vec2{x: x, y: y}

define dot(ax: int, ay: int, bx: int, by: int) -> int:
    ax * bx + ay * by

define main() -> int:
    v = make_vec(3, 4)
    d = dot(v.x, v.y, v.x, v.y)
    d
