data Vec2: x: int, y: int

fn make_vec(x: int, y: int) -> Vec2:
    Vec2{x: x, y: y}

fn dot(ax: int, ay: int, bx: int, by: int) -> int:
    ax * bx + ay * by

fn main() -> int:
    v = make_vec(3, 4)
    d = dot(v.x, v.y, v.x, v.y)
    d
