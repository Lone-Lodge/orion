# expect: yield` outside a collecting loop
# `yield` only means something inside `x = loop v in seq:` with an indented
# body. On its own it must fail loud, not silently vanish.
fn main() -> int:
    yield 5
    0
