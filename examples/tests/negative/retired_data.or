# expect: `data` was retired
# `type` is the one type-declaration keyword: this product used to be `data`.
data Point: x: number, y: number

define main() -> number:
    p = Point{x: 1, y: 2}
    p.x + p.y
