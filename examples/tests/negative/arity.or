# expect: arity.or:7:4 — `add` takes 2 argument(s), got 1
define add(a: int, b: int) -> int:
    a + b

define main() -> int:
    # one argument short: a silent zero here would be a miscompile
    add(1)
