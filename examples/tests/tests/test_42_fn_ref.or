define double(x: int) -> int:
    x * 2

# First-class fn ref: bind a global fn to a local var, call indirectly.
# Foundation for closures (no captures yet).
define main() -> int:
    f = double
    f(21)
