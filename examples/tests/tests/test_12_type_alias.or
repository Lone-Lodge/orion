# Type alias: `type Name = T` gives an existing type a second name. `Celsius`
# resolves to `int` at lowering, in both a return type and a typed binding.
type Celsius = int

fn make() -> Celsius:
    5

fn main() -> int:
    mut t: Celsius = make()
    t + 7    # 12
