# Immutable typed binding `name: Type = expr` — the symmetric partner of
# `mut name: Type = expr`. Works with a builtin type and a type alias.
type Meters = int

define main() -> int:
    x: int = 40
    y: Meters = 3
    x + y      # 43
