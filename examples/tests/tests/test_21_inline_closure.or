# A capturing lambda passed DIRECTLY as a call argument (not bound first) -
# lifted by the nested-lambda pass. k=7 captured; f(3) = 3 * 7 = 21.
define apply(f: function, x: int) -> int:
    f(x)
define main() -> int:
    k = 7
    apply(function(n): n * k, 3)
