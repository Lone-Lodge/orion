define day_value(day: text) -> int:
    choose day:
        "mon" -> 10
        "fri" -> 32
        _ -> 0

define level_value(level: int) -> int:
    choose level:
        0 -> 0
        1 -> 100
        _ -> 200

define main() -> int:
    day_value("fri") + level_value(1) - level_value(1) + 10
