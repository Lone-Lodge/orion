# Out-of-range list indexing used to load adjacent memory and return
# garbage with exit 0 (a silent wrong answer). It now traps loudly:
# prints `orion: list index N out of range (len L)` and exits 70.
fn main() -> int:
    xs = [10, 20, 30]
    xs[9]
