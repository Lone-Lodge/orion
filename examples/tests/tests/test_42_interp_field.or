type Point: x: int, y: int

define main() -> int:
    p = Point{x: 18, y: 24}
    # Interpolation with field access
    msg = "p.x={p.x} p.y={p.y}"
    if contains(msg, "p.x=18"):
        p.x + p.y
    else:
        0
