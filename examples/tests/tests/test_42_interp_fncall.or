define double(x: number) -> number:
    x + x

define main() -> number:
    # Interpolation with fn call
    msg = "double of 21 is {double(21)}"
    if contains(msg, "is 42"):
        42
    else:
        0
