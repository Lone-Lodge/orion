define main() -> int:
    # Inline lambda - `function(params): body`. Lifted to a synthesized fn at
    # compile time.
    double = function(x): x * 2
    inc = function(x): x + 1
    # 10 -> double(10)=20 -> inc(20)=21 -> 21*2=42
    a = double(10)
    b = inc(a)
    b * 2
