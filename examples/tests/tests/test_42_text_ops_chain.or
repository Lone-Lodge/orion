define main() -> int:
    base = "hello world!"
    n = length(base)
    # length("hello world!") = 12
    sub = part(base, 6, 11)  # "world"
    # contains("world", "or") = true
    if contains(sub, "or"):
        # 12*3 = 36 + 6 = 42
        n * 3 + 6
    else:
        0
