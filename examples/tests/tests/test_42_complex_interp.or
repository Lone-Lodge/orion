type Item: name: Text, value: int

fn describe(item: Item) -> Text:
    "{item.name}: {item.value}"

fn main() -> int:
    a = Item{name: "x", value: 21}
    msg = describe(a)
    # describe should produce "x: 21"
    if contains(msg, "x: 21"):
        a.value + a.value
    else:
        0
