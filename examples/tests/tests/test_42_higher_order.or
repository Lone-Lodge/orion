define double(x: number) -> number:
    x * 2

# Higher-order fn - accepts another fn as a parameter (type: function).
define apply_twice(f: function, x: number) -> number:
    f(f(x))

define main() -> number:
    apply_twice(double, 10) + 2
