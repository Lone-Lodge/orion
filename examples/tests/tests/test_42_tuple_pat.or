# Tuple patterns in match: `(a, b) -> ...` destructures a tuple, binding each
# field with its real type (heterogeneous: n is int, s is text).
define describe(p: (int, text)) -> int:
    choose p:
        (n, s) -> n + len(s)

define main() -> int:
    describe((37, "abcde"))    # 37 + 5 == 42
