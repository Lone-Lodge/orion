# Anonymous fn / lambda in expression position: `function(params): body`.
# A newcomer who can read `define add(n): ...` can read this. Same node as |n|.
define apply(f: function, x: number) -> number:
    f(x)
define main() -> number:
    add1 = function(n): n + 1
    apply(add1, 41)
