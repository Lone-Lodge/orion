type Option: has: int, value: int

define Some(v: int) -> Option:
    Option{has: 1, value: v}

define None() -> Option:
    Option{has: 0, value: 0}

define find_in_list(xs: [int], target: int) -> Option:
    edit idx = 0
    edit found = None()
    loop x in xs:
        if x == target:
            found = Some(idx)
        idx = idx + 1
    found

define main() -> int:
    items = [10, 21, 30, 21]
    a = find_in_list(items, 21)
    b = find_in_list(items, 99)
    # a.value = 1 (idx of first 21)
    # b.has = 0 (not found)
    # 1 * 21 + (1 - 0) * 21 = 42
    if a.has == 1 and b.has == 0:
        a.value * 21 + 21
    else:
        0
