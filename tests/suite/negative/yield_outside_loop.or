# expect: yield` outside a collecting loop
# `collect` only means something inside `x = loop v in seq:` with an indented
# body. On its own it must fail loud, not silently vanish.
define main() -> number:
    collect 5
    0
