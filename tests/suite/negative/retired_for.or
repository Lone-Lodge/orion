# expect: retired_for.or:7:4 - `for` was retired
# The loop keyword is `loop`. `for` used to be an accepted alias; the corpus was
# migrated 2026-07-26 and the alias removed, so this must fail LOUDLY rather
# than parse `for` as a bare identifier.
define main() -> number:
    edit total = 0
    for i in 0 until 3:
        total += i
    total
