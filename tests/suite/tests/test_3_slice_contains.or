define main() -> number:
    s = "hello world"
    edit score = 0
    if contains(s, "world"):
        score = score + 1
    if not contains(s, "xyz"):
        score = score + 1
    sub = part(s, 6, 11)
    if sub is "world":
        score = score + 1
    score
