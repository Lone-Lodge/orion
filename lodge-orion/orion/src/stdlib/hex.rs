//! `hex` — lowercase hex encode/decode for byte arrays.
//!
//! Pure Orion stub — implementations live in `orbs/hex/lib.or` and are
//! intentionally left as TODOs for the user to fill in. See
//! `orbs/hex/LEARN.md` for the guided walkthrough.

use crate::interp::Interp;

pub const SOURCE: &str = include_str!("../../../orbs/hex/lib.or");

pub fn register(_interp: &Interp) {}
